name: CI

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  wrapper-validation:
    name: Validate Gradle Wrapper
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4.2.2

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4.3.1

  build:
    name: Build And Test
    runs-on: ubuntu-latest
    needs: wrapper-validation

    steps:
      - name: Check out repository
        uses: actions/checkout@v4.2.2

      - name: Set up Java 21
        uses: actions/setup-java@v4.7.0
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle cache
        uses: gradle/actions/setup-gradle@v4.3.1

      - name: Make Gradle wrapper executable
        run: chmod +x ./gradlew

      - name: Build project and addons
        run: ./gradlew --no-daemon build :enhancements-addon:build :proxy-addon:build

      - name: Upload plugin jars
        if: success()
        uses: actions/upload-artifact@v4.6.2
        with:
          name: openeco-jars
          path: |
            build/libs/*.jar
            enhancements-addon/build/libs/*.jar
            proxy-addon/build/libs/*.jar
          if-no-files-found: error

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: openeco-test-reports
          path: |
            build/reports/tests/test/**
            build/test-results/test/**
            enhancements-addon/build/reports/tests/test/**
            enhancements-addon/build/test-results/test/**
            proxy-addon/build/reports/tests/test/**
            proxy-addon/build/test-results/test/**
          if-no-files-found: warn